const string: string = "5";
const number: number = 5;
const boolean: boolean = true;

const myFunction: (variable: number, variable2: string) => void = (
  variable,
  variable2
) => {};

const myFunction2: (variable: number, variable2: string) => void = (
  variable,
  variable2
) => {
  // fonksiyon içi
};

const myFunction3: (variable: string, variable2: number) => void = (
  variable,
  variable2
) => {};

const dizi:string[]=[]